name: poc-bicep-$(workloadName)-$(Date:yyyyMMdd)$(Rev:.r)

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - infra/**
      - scripts/**
      - pipelines/**
    exclude:
      - docs/**

pr:
  branches:
    include:
      - main
  paths:
    include:
      - infra/**
      - scripts/**
      - pipelines/**
    exclude:
      - docs/**

variables:
  workloadName: avd
  environmentName: poc
  azureDevOpsEnvironment: poc-avd
  azureServiceConnection: tv-avd-poc-deploy-sc
  deploymentLocation: eastus

pool:
  vmImage: ubuntu-latest

stages:
  - template: templates/bicep-deployment-template.yml
    parameters:
      deploymentUnitName: bootstrap
      azureDevOpsEnvironment: $(azureDevOpsEnvironment)
      azureServiceConnection: $(azureServiceConnection)
      environmentName: poc
      workloadName: $(workloadName)-bootstrap
      deploymentScope: Subscription
      deploymentLocation: $(deploymentLocation)
      templateFile: "$(Build.SourcesDirectory)/infra/modules/bootstrap/main.bicep"
      parameterFile: "$(Build.SourcesDirectory)/infra/modules/bootstrap/poc.bicepparam"
      validationLevel: Provider
      whatIfResultFormat: FullResourcePayloads

  - template: templates/bicep-deployment-template.yml
    parameters:
      deploymentUnitName: serviceobjects
      deployDependsOn:
        - Deploy_bootstrap
      azureDevOpsEnvironment: $(azureDevOpsEnvironment)
      azureServiceConnection: $(azureServiceConnection)
      environmentName: poc
      workloadName: $(workloadName)-serviceobjects
      deploymentScope: Subscription
      deploymentLocation: $(deploymentLocation)
      templateFile: "$(Build.SourcesDirectory)/infra/modules/serviceobjects/main.bicep"
      parameterFile: "$(Build.SourcesDirectory)/infra/modules/serviceobjects/poc.bicepparam"
      validationLevel: Provider
      whatIfResultFormat: FullResourcePayloads

  - template: templates/bicep-deployment-template.yml
    parameters:
      deploymentUnitName: network
      validationDependsOn:
        - Deploy_bootstrap
      deployDependsOn:
        - Deploy_bootstrap
      azureDevOpsEnvironment: $(azureDevOpsEnvironment)
      azureServiceConnection: $(azureServiceConnection)
      environmentName: poc
      workloadName: $(workloadName)-network
      deploymentScope: Subscription
      deploymentLocation: $(deploymentLocation)
      templateFile: "$(Build.SourcesDirectory)/infra/modules/network/main.bicep"
      parameterFile: "$(Build.SourcesDirectory)/infra/modules/network/poc.bicepparam"
      validationLevel: Provider
      whatIfResultFormat: FullResourcePayloads

  - template: templates/bicep-deployment-template.yml
    parameters:
      deploymentUnitName: storage
      validationDependsOn:
        - Deploy_bootstrap
      deployDependsOn:
        - Deploy_bootstrap
      azureDevOpsEnvironment: $(azureDevOpsEnvironment)
      azureServiceConnection: $(azureServiceConnection)
      environmentName: poc
      workloadName: $(workloadName)-storage
      deploymentScope: Subscription
      deploymentLocation: $(deploymentLocation)
      templateFile: "$(Build.SourcesDirectory)/infra/modules/storage/main.bicep"
      parameterFile: "$(Build.SourcesDirectory)/infra/modules/storage/poc.bicepparam"
      validationLevel: Provider
      whatIfResultFormat: FullResourcePayloads

  - template: templates/bicep-deployment-template.yml
    parameters:
      deploymentUnitName: storageauth
      validationDependsOn:
        - Deploy_serviceobjects
        - Deploy_network
        - Deploy_storage
      deployDependsOn:
        - Deploy_serviceobjects
        - Deploy_network
        - Deploy_storage
      azureDevOpsEnvironment: $(azureDevOpsEnvironment)
      azureServiceConnection: $(azureServiceConnection)
      environmentName: poc
      workloadName: $(workloadName)-storageauth
      deploymentScope: Subscription
      deploymentLocation: $(deploymentLocation)
      templateFile: "$(Build.SourcesDirectory)/infra/modules/storage-auth/main.bicep"
      parameterFile: "$(Build.SourcesDirectory)/infra/modules/storage-auth/poc.bicepparam"
      validationLevel: Provider
      whatIfResultFormat: FullResourcePayloads

  - template: templates/bicep-deployment-template.yml
    parameters:
      deploymentUnitName: compute
      validationDependsOn:
        - Deploy_storageauth
      deployDependsOn:
        - Deploy_storageauth
      azureDevOpsEnvironment: $(azureDevOpsEnvironment)
      azureServiceConnection: $(azureServiceConnection)
      environmentName: poc
      workloadName: $(workloadName)-compute
      deploymentScope: Subscription
      deploymentLocation: $(deploymentLocation)
      templateFile: "$(Build.SourcesDirectory)/infra/modules/compute/main.bicep"
      parameterFile: "$(Build.SourcesDirectory)/infra/modules/compute/poc.bicepparam"
      validationLevel: Provider
      whatIfResultFormat: FullResourcePayloads
